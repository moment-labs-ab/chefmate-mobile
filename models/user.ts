
export type User = {
    id: string,
    username?: string,
    firstName?: string,
    lastName?: string,
    email : string,
    timeCreated?: number,
    timeUpdated?: number,
    avatar?: string,
};